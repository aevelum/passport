# Daml Script Tests

The Foundation Release test suite treats Daml scripts as executable acceptance criteria.
The named scripts are the gate definitions for the first Canton committee demo.
Temporal tests cover typed `Time` policy validity, credential freshness, presentation expiry, reservation expiry, and legacy text timestamp metadata behavior.
